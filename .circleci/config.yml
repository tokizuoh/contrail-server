version: 2.1

jobs:
  dispatch-schema-to-client:
    docker:
      - image: cimg/go:1.18.4
    steps:
      - checkout
      - run:
          name: "go run server.go"
          command: "go run server.go &"
      - run: sleep 5

workflows:
  say-hello-workflow:
    jobs:
      - dispatch-schema-to-client